// Shared typography tokens. Brand colors live as CSS vars in index.css
// (--brand, --brand-light, --brand-dark) so Tailwind arbitrary values can
// reference them directly without a JS round-trip.

export const FONT = {
  display: '"Schibsted Grotesk", sans-serif',
  sans: '"Hanken Grotesk", system-ui, sans-serif',
};
